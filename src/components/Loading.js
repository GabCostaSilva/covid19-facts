import React from "react"

const Loading = () => (
    <div
        style={{
            display: "flex",
            alignItems: "center",
            justifyContent: "center",
        }}
    >
        carregando...
    </div>
)
export default Loading